# Decibel pre-deposit script — copy to .env and fill

# Aptos wallet private keys (hex, comma-separated). One deposit per wallet.
PRIVATE_KEYS=

# Geomi Client API key — for Decibel GET APIs (subaccounts). Get at https://geomi.dev
APTOS_NODE_API_KEY=

# Optional: USDC fungible asset metadata address (if not using SDK default for Netna)
# DECIBEL_USDC_METADATA=

# Amount in USDC to deposit per wallet (min 50, max 1_000_000 per Decibel rules)
DEPOSIT_USDC=50

# true = Aptos Testnet, false = Netna
DECIBEL_TESTNET=false

# Optional: referral code from pre-deposits page
# DECIBEL_REFERRAL_CODE=

# --- Solana CCTP (deposit-solana.ts): только SOL на газ, USDC на Solana → Aptos ---
# SOLANA_PRIVATE_KEY=   # base58 или JSON-массив секретного ключа
# APTOS_RECIPIENT_ADDRESS=   # Aptos-адрес (0x + 64 hex), куда минтить USDC
# SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
# DEPOSIT_USDC=50   # используется и для Aptos, и для Solana
